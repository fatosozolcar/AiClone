@using GptApp.Domain

@if (ChatMessage.MessageType == GptChatMessageType.AI || ChatMessage.MessageType == GptChatMessageType.Sytem){

    <div class="chat chat-start">
                <div class="chat-image avatar">
                    <div class="w-10 rounded-full">
                        <img src="../wwwroot/ai-avatar.png" alt="AI Avatar">
                    </div>
                </div>
                <div class="chat-bubble chat-bubble-secondary">@ChatMessage.Message</div>
                <div class="chat-footer opacity-50">@ChatMessage.CreatedOn.ToShortTimeString</div>
               </div>
}

@if(ChatMessage.MessageType == GptChatMessageType.User){
    <div class="chat chat-end">
                <div class="chat-image avatar">
                    <div class="div w-10 rounded-full">
                        <img src="../wwwroot/user-avatar.png" alt="User Avatar">
                    </div>
                    <div class="chat-bubble chat-bubble-secondary">@ChatMessage.Message</div>
                    <div class="chat-footer opacity-50">@ChatMessage.CreatedOn.ToShortTimeString</div>
                </div>
    </div>
}


@code {
[Parameter] public GptChatMessage ChatMessage {get; set; }

}
